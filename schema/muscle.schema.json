{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://nmsk-atlas/schema/muscle.schema.json",
  "title": "Muscle",
  "description": "A named muscle, decomposed into one or more functional compartments (neuromuscular partitions). Each compartment carries a parametric fiber-architecture description consumed by engine/fiber_field.py to procedurally generate a 1mm-resolution vector field of individual fascicle centerlines between origin and insertion surfaces \u2014 this is how per-fiber-bundle direction and per-compartment grouping is produced without hand-authoring millions of fiber curves.",
  "type": "object",
  "properties": {
    "id": {
      "type": "string"
    },
    "name_ta": {
      "type": "string"
    },
    "name_common": {
      "type": "string"
    },
    "side": {
      "$ref": "common.schema.json#/definitions/side"
    },
    "region": {
      "type": "string"
    },
    "overall_architecture_type": {
      "type": "string",
      "enum": [
        "parallel_strap",
        "fusiform",
        "unipennate",
        "bipennate",
        "multipennate",
        "circular",
        "convergent_triangular",
        "digastric"
      ]
    },
    "attachments": {
      "type": "object",
      "properties": {
        "origin_bone": {
          "type": "string"
        },
        "origin_landmark": {
          "type": "string"
        },
        "insertion_bone": {
          "type": "string"
        },
        "insertion_landmark": {
          "type": "string"
        },
        "via_points": {
          "type": "array",
          "description": "Wrapping/via points (retinacula, bony pulleys, sesamoids) the muscle-tendon path is constrained through, so its path deforms plausibly rather than penetrating bone during joint rotation.",
          "items": {
            "type": "object",
            "properties": {
              "structure": {
                "type": "string"
              },
              "bone_frame": {
                "type": "string",
                "description": "id of the bone whose local frame this via-point is rigidly attached to"
              },
              "position_local_mm": {
                "$ref": "common.schema.json#/definitions/vec3_mm"
              },
              "active_over_joint_range": {
                "type": "string"
              }
            }
          }
        }
      },
      "required": [
        "origin_bone",
        "origin_landmark",
        "insertion_bone",
        "insertion_landmark"
      ]
    },
    "innervation": {
      "type": "object",
      "properties": {
        "nerve": {
          "type": "string"
        },
        "root_levels": {
          "type": "string"
        }
      },
      "required": [
        "nerve",
        "root_levels"
      ]
    },
    "blood_supply": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "actions": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "functional_compartments": {
      "type": "array",
      "description": "Independently-innervated or mechanically-separable fiber bundle groups within this single named muscle (e.g. deltoid's ~7 segments, biceps femoris' two heads). A muscle with a single homogeneous fiber population still gets exactly one compartment entry.",
      "items": {
        "type": "object",
        "properties": {
          "id": {
            "type": "string"
          },
          "name": {
            "type": "string"
          },
          "innervation_branch": {
            "type": "string",
            "description": "specific nerve branch/twig serving only this compartment, when documented"
          },
          "fiber_architecture": {
            "type": "object",
            "properties": {
              "architecture_type": {
                "type": "string",
                "enum": [
                  "parallel_strap",
                  "fusiform",
                  "unipennate",
                  "bipennate",
                  "multipennate",
                  "circular",
                  "convergent_triangular",
                  "digastric"
                ]
              },
              "pennation_deg": {
                "type": "number"
              },
              "optimal_fascicle_length_mm": {
                "type": "number"
              },
              "physiological_cross_section_area_mm2": {
                "type": "number"
              },
              "tendon_slack_length_mm": {
                "type": "number"
              },
              "max_isometric_force_N": {
                "type": "number"
              },
              "aponeurosis_definition": {
                "type": "string",
                "description": "how the internal aponeurosis surface(s) that fibers attach to are defined, for pennate muscles"
              }
            },
            "required": [
              "architecture_type"
            ]
          },
          "fiber_field_seed_region_local_mm": {
            "type": "array",
            "description": "Polygon (list of vec3_mm, in origin_bone's local frame) bounding the origin footprint of this compartment's fibers on the origin surface \u2014 the seed curve fiber_field.py sows fascicle start points along.",
            "items": {
              "$ref": "common.schema.json#/definitions/vec3_mm"
            }
          },
          "neuromuscular_junction_zone": {
            "type": "object",
            "description": "The NMJ/motor endplate band is a localized transverse zone (not point-uniform) documented for most human muscles, typically near the fascicle midpoint / muscle belly center.",
            "properties": {
              "location_description": {
                "type": "string"
              },
              "position_fraction_along_fascicle": {
                "type": "number",
                "minimum": 0,
                "maximum": 1,
                "description": "0=origin end, 1=insertion end; ~0.5 for most parallel/fusiform muscles per endplate-banding studies"
              },
              "band_width_fraction": {
                "type": "number",
                "description": "width of the endplate zone as a fraction of total fascicle length"
              },
              "evidence": {
                "type": "string",
                "enum": [
                  "measured",
                  "modelling_default"
                ],
                "description": "'measured' means position_fraction_along_fascicle is taken from a study of this muscle. 'modelling_default' means it is the generic mid-belly assumption (0.5) applied because no muscle-specific measurement has been entered -- see motor_endplate_zones for published, landmark-referenced data where it exists."
              }
            }
          },
          "function_note": {
            "type": "string"
          },
          "source": {
            "$ref": "common.schema.json#/definitions/citation"
          },
          "motor_endplate_zones": {
            "type": "array",
            "description": "Published motor endplate / intramuscular nerve arborization zones, recorded IN THE SOURCE'S OWN TERMS: a percentage range along a named line between two external landmarks. Deliberately NOT converted into position_fraction_along_fascicle -- the published reference lines run in varying directions relative to each muscle's origin-to-insertion axis (several run distal-to-proximal), so a silent conversion would invert some zones. It is also the form a clinician actually palpates and measures against.",
            "items": {
              "type": "object",
              "required": [
                "zone_percent_range",
                "reference_line_from",
                "reference_line_to",
                "source"
              ],
              "properties": {
                "zone_percent_range": {
                  "type": "array",
                  "items": {
                    "type": "number",
                    "minimum": 0,
                    "maximum": 100
                  },
                  "minItems": 2,
                  "maxItems": 2,
                  "description": "[low, high] percent along the reference line, measured from reference_line_from (0%) toward reference_line_to (100%)."
                },
                "reference_line_from": {
                  "type": "string",
                  "description": "the 0% landmark"
                },
                "reference_line_to": {
                  "type": "string",
                  "description": "the 100% landmark"
                },
                "method": {
                  "type": "string",
                  "description": "how the zone was determined, e.g. Sihler stain arborization mapping, EMG innervation-zone imaging, cadaveric dissection"
                },
                "notes": {
                  "type": "string"
                },
                "source": {
                  "type": "string"
                }
              }
            }
          }
        },
        "required": [
          "id",
          "name",
          "fiber_architecture",
          "source"
        ]
      }
    },
    "source": {
      "$ref": "common.schema.json#/definitions/citation"
    },
    "ultrasound_injection_approach": {
      "type": "object",
      "description": "How this muscle is found and safely injected under ultrasound. Distinct from motor_endplate_zones: a zone says WHERE ALONG the muscle the endplate band lies, while this says how to bring the probe and needle to it and what must not be hit on the way. Both are needed before an injection is planned.",
      "required": [
        "transducer_placement",
        "source"
      ],
      "properties": {
        "transducer_placement": {
          "type": "string",
          "description": "probe position and orientation, referenced to palpable landmarks"
        },
        "depth_layer": {
          "type": "string",
          "description": "which compartment layer the muscle occupies and what lies superficial and deep to it"
        },
        "identification_cues": {
          "type": "string",
          "description": "sonographic appearance and dynamic manoeuvres that confirm the muscle"
        },
        "structures_at_risk": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "nerves and vessels close enough to the needle path to matter"
        },
        "notes": {
          "type": "string"
        },
        "source": {
          "type": "string"
        }
      }
    }
  },
  "required": [
    "id",
    "name_ta",
    "attachments",
    "innervation",
    "actions",
    "functional_compartments",
    "source"
  ]
}
