numpy>=1.24
jsonschema>=4.0
pytest>=7.0

# Only for scripts/ingest_volume_geometry.py -- reading a segmented CT or MRI
# and surfacing its labels. The STL ingest, the engine and every audit run
# without them, and their tests skip when they are absent.
nibabel>=5.0
scikit-image>=0.20
trimesh>=4.0
shapely>=2.0
rtree>=1.0
fast-simplification>=0.1   # quadric decimation of thin sheets for the viewer bundle (scripts/export_viewer_bundle.py)
