numpy>=1.24
jsonschema>=4.0
pytest>=7.0

# Only for scripts/ingest_volume_geometry.py -- reading a segmented CT or MRI
# and surfacing its labels. The STL ingest, the engine and every audit run
# without them, and their tests skip when they are absent.
nibabel>=5.0
scikit-image>=0.20
